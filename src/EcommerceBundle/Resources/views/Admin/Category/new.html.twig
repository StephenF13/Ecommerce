{% extends 'EcommerceBundle:Admin:layoutAdmin.html.twig' %}

{% block body %}
    <h1>Création de catégorie</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('adminCategory_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
